# 同学一 (Person 1) 职责与开发指南
## — 用户登录、安全校验与账户管理模块 —

本文件详细阐述同学一在项目中的代码所有权、核心实现逻辑，以及在系统运行时通过终端交互可看到的操作界面。

---

## 1. 负责的代码文件列表 (代码所有权)
为了避免与其他成员的代码发生冲突，同学一在开发期间**拥有以下文件的唯一修改权**：

### 📁 实体类层 (Entity)
* [`src/com/sms/entity/User.java`](file:///d:/Dev/shixun/src/com/sms/entity/User.java) (用户账户实体类)
* [`src/com/sms/entity/UserRole.java`](file:///d:/Dev/shixun/src/com/sms/entity/UserRole.java) (角色枚举类：`ADMIN` / `TEACHER` / `STUDENT`)

### 📁 核心工具类 (Util)
* [`src/com/sms/util/MD5Util.java`](file:///d:/Dev/shixun/src/com/sms/util/MD5Util.java) (安全加解密工具，包括散列盐值计算)

### 📁 数据访问与业务服务层 (DAO & Service)
* `src/com/sms/dao/UserDAO.java` 与 `UserDAOImpl.java`
* `src/com/sms/service/UserService.java` 与 `UserServiceImpl.java`

### 📁 启动入口与终端控制 (Entry & Controller 独占方法)
* [`src/com/sms/App.java`](file:///d:/Dev/shixun/src/com/sms/App.java) (程序主入口，含登录界面、密码安全隐藏输入、验证逻辑和三端分发路由)
* **管理员端** ([AdminController.java](file:///d:/Dev/shixun/src/com/sms/controller/AdminController.java)) 中的账户控制方法：
  * `manageAccounts()` (账户与安全管理大菜单)
  * `toggleAccountStatus()` (启用或禁用特定账户/解锁被锁定账号)
  * `resetAccountPassword()` (强制重置其他用户的密码)

---

## 2. 核心业务与实现逻辑

### 2.1 密码安全散列存储 (MD5 + Salt)
数据库的 `user` 表中**绝对不存储任何明文密码**，密码通过以下公式生成后存入 `password_hash`：
$$\text{password\_hash} = \text{MD5}(\text{salt} + \text{password})$$
* **登录校验逻辑**：
  1. 根据输入的用户名查询对应的用户信息（获取 `password_hash` 和随机分配的 `salt`）。
  2. 使用相同的 `salt` 加密输入的明文密码，与数据库存入的 Hash 值做对比。
  3. 比对成功则通过，不成功则累计失败次数。

### 2.2 账户锁定防暴力破解机制
为了提高系统安全级别，限制非法猜解密码：
* 当用户连续输错密码时，系统必须记录连续失败次数。
* **连续登录失败满 5 次**，该账户的激活状态自动变更为 **`0` (禁用/锁定)**。
* 账号锁定后，即使输入正确密码也无法登录，控制台将报错：`该账户已锁定，请联系管理员解锁！`。
* 锁定后的账户**只能由管理员**在后台进行 `启用/解锁` 操作，或者由管理员 `重置密码` 后恢复。

### 2.3 终端密码安全输入隐藏
为了防窥，同学一需要确保登录时的密码输入不可见：
* 使用 `System.console().readPassword()` 获取密码，这样在标准的操作系统终端（如 Cmd/PowerShell）输入密码时，屏幕上**不会显示任何字符**。
* 做好非终端环境的兼容，如果 `System.console()` 为 `null`（如在某些 IDE 运行窗口），则降级使用常规输入，确保程序不会抛空指针异常。

---

## 3. 操作运行时能看到什么 (终端界面展示)

### 3.1 用户登录与防暴力破解展示
1. 启动系统后，进入主界面：
   ```text
   ==================================================
                    用户登录界面                     
   ==================================================
   请输入用户名 > student1
   请输入密码 > ****** (此时屏幕不回显任何明文，光标保持原样)
   ```
2. 若输入错误：
   ```text
   [错误] 登录失败: 密码错误！您还有 4 次尝试机会，连续失败 5 次账户将被锁定。
   ```
3. 连续失败 5 次后：
   ```text
   [错误] 登录失败: 密码错误！该账户已达到最大失败次数，已被安全锁定，请联系管理员！
   ```

### 3.2 管理员账户控制中心
管理员进入菜单 `[8] 账户与安全管理`：
1. **查看所有账户状态**：
   ```text
   ==================================================
     请选择目标账户
   ==================================================
     [1] 用户:admin | 姓名:系统管理员 | 角色:ADMIN | 状态:启用
     [2] 用户:teacher1 | 姓名:王建国 | 角色:TEACHER | 状态:启用
     [3] 用户:student1 | 姓名:李明 | 角色:STUDENT | 状态:禁用 (锁定)
   --------------------------------------------------
   ```
2. **启用/禁用账户**：
   管理员选择 `3` 号学生账户：
   ```text
   确认要 启用 账户 student1 吗？ (y/n) > y
   [成功] 操作成功！该账户已启用
   ```
3. **重置账户密码**：
   ```text
   请输入新密码 > newpass123
   确认将账户 [student1] 密码重置？ (y/n) > y
   [成功] 密码重置成功！ (系统会自动生成新的 Salt 并写入重新加密的 MD5)
   ```

---
* 同学一可以根据此文档，清晰地认领自己需要负责的模块，编写对应的安全校验和登录分发控制器。
