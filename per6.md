# 同学六 (Person 6) 职责与开发指南
## — 班级管理、教学计划与排课课表模块 —

本文件详细阐述同学六在项目中的代码所有权、核心实现逻辑，以及在系统运行时通过终端交互可看到的操作界面。

---

## 1. 负责的代码文件列表 (代码所有权)
为了避免与其他成员的代码发生冲突，同学六在开发期间**拥有以下文件的唯一修改权**：

### 📁 实体类层 (Entity)
* [`src/com/sms/entity/Clazz.java`](file:///d:/Dev/shixun/src/com/sms/entity/Clazz.java) (班级实体)
* [`src/com/sms/entity/TeachingPlan.java`](file:///d:/Dev/shixun/src/com/sms/entity/TeachingPlan.java) (教学计划实体)
* [`src/com/sms/entity/Schedule.java`](file:///d:/Dev/shixun/src/com/sms/entity/Schedule.java) (课表排课实体)

### 📁 数据访问层 (DAO)
* `src/com/sms/dao/ClazzDAO.java` 与 `ClazzDAOImpl.java`
* `src/com/sms/dao/TeachingPlanDAO.java` 与 `TeachingPlanDAOImpl.java`
* `src/com/sms/dao/ScheduleDAO.java` 与 `ScheduleDAOImpl.java`

### 📁 业务服务层 (Service)
* `src/com/sms/service/AcademicService.java` 与 `AcademicServiceImpl.java` (全权负责管理班级、教学计划和课表的所有后台逻辑)

### 📁 终端控制层 (Controller 独占方法)
* **管理员端** ([AdminController.java](file:///d:/Dev/shixun/src/com/sms/controller/AdminController.java)) 中的方法：
  * `manageClasses()` (班级管理菜单及子功能)
  * `manageTeachingPlans()` (教学计划管理菜单及子功能)
  * `manageSchedules()` (排课管理菜单及子功能)
* **教师端** ([TeacherController.java](file:///d:/Dev/shixun/src/com/sms/controller/TeacherController.java)) 中的方法：
  * `viewMySchedule()` (查看我的授课日程表)
* **学生端** ([StudentController.java](file:///d:/Dev/shixun/src/com/sms/controller/StudentController.java)) 中的方法：
  * `viewMyTimetable()` (查看我的班级课表)

---

## 2. 核心业务与实现逻辑

### 2.1 班级管理系统
同学六负责在管理员端管理班级的完整生命周期：
* **班级列表展示**：查询所有班级信息，包括班级编号、名称、所属专业、入学年份、学生人数，以及班主任的名字。
* **设置与更换班主任**：允许管理员在教师档案库中选择一名教师，将其绑定到某个班级担任班主任（修改 `class.head_teacher_id`）。
* **班级花名册**：根据班级ID，拉取该班级的全部学生，并以表格形式在终端展示。

### 2.2 教学计划管理
教学计划定义了**“谁（教师）在哪个学期（semester）教哪个班级（班级ID）的哪门课（课程ID）”**。
* 注意：如果 `class_id` 为 `NULL`，则表示该教学计划是一门公选课或自由选修课，全校学生均可自由选修。

### 2.3 🌟 智能排课算法与冲突检测 (开发难点)
在管理员对某项教学计划进行排课时，同学六必须编写**冲突检测逻辑**，以防出现教学事故。
当向数据库插入一条排课记录 `Schedule` 时，必须进行如下三项强校验：
1. **教室冲突校验**：检测在同一个学期、相同的星期、且上课节次重叠的情况下，该**多媒体教室/机房**是否已经被其他班级占用。
2. **教师冲突校验**：检测该**任课教师**是否在同一时间段已经被安排在别处上课。
3. **班级冲突校验**：检测该**行政班级**的学生是否在同一时间段已经被排了其他的课程。

**重叠区间判断算法**：
若已有课程节次为 `[startA, endA]`，新排课程节次为 `[startB, endB]`，重叠的数学条件为：
$$\max(startA, startB) \le \min(endA, endB)$$
如果满足该条件，则说明时间段重合，系统必须报错拦截，禁止排课。

---

## 3. 操作运行时能看到什么 (终端界面展示)

### 3.1 管理员视角 (Admin)
* **进入 [3] 班级管理**：可以看到一个精心排版的班级列表表格：
  ```text
  ----------------------------------------------------------------------
  班级编号   | 班级名称      | 所属专业       | 班主任   | 学生人数
  ----------------------------------------------------------------------
  C2301      | 计算机2301班  | 计算机科学     | 王建国   | 8 人
  C2302      | 软件2301班    | 软件工程       | 刘秀英   | 6 人
  ----------------------------------------------------------------------
  ```
  管理员可以选择“设置班主任”或“查看班级花名册”展示该班所有学生。
  
* **进入 [6] 课表排课管理**：系统会提示选择一个教学计划，然后录入排课信息（如选择“周二第3-4节，机房-502，主校区”）。
  * 若无冲突：系统显示 `[成功] 排课成功！`。
  * 若有冲突：系统显示红色的错误提示，例如 `[错误] 排课冲突：教师王建国在周二第3-4节已安排有[Java程序设计]课！`。

### 3.2 教师视角 (Teacher)
* **进入 [1] 查看我的授课日程**：教师可以清晰直观地以星期排序的 ASCII 表格形式看到自己本学期的排课分布：
  ```text
  ----------------------------------------------------------------------------
  星期     | 时间段       | 授课科目        | 授课班级     | 教室       | 校区
  ----------------------------------------------------------------------------
  星期一   | 第 1-2 节    | 高等数学        | 计算机2301   | 教三-301   | 主校区
  星期二   | 第 3-4 节    | Java程序设计    | 计算机2301   | 机房-502   | 主校区
  ----------------------------------------------------------------------------
  ```

### 3.3 学生视角 (Student)
* **进入 [3] 查看我的班级课表**：系统会自动抓取该学生所在班级的所有课表日程，并按**星期一到星期日**分组展开展示，一目了然：
  ```text
  ● 星期一 (Monday)
    [第1-2节] 高等数学 | 老师: 张爱华 | 教室: 教三-301 (主校区)
  ● 星期二 (Tuesday)
    [第3-4节] Java程序设计 | 老师: 王建国 | 教室: 机房-502 (主校区)
  ```
